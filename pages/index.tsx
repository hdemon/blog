import { css } from "@emotion/react";

export default function Home() {
  return (
    <div
      css={css`
        background: blue;
        width: 10px;
        height: 10px;
        display: block;
      `}
    />
  );
}
